# Default environment variables
# These can be overridden by creating a .env.local file

# Browser for tests (chromium, firefox, webkit)
BROWSER=chromium

# Viewport for tests (desktop, tablet, mobile)
VIEWPORT=desktop

# Run in headless mode (true or false)
HEADLESS=true

# Enable Playwright tracing (true or false)
TRACE_ENABLED=false
